# Ignore everything by default
*

# Allow directories so Git can descend into them
!*/

# Keep Python source files
!*.py

# Keep parameters_used
!*.yaml
!*.json

# Keep this .gitignore itself
!.gitignore
